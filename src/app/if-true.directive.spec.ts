import { IfTrueDirective } from './if-true.directive';

describe('IfTrueDirective', () => {
  it('should create an instance', () => {
    const directive = new IfTrueDirective();
    expect(directive).toBeTruthy();
  });
});
